# Captured frames and per-game statistics are the player's own, not the
# project's - CLAUDE.md describes both as gitignored,
captures/
stats/

# My explainer doc and other personal working notes, kept out of the repo
# deliberately.
.loom-notes.md
.loom-roadmap.md
.video-checklist.md
.agent-onboarding.md

# Local AI assistant state.
.claude/

# Scratch frame written by the debug tools.
_debug.png

# The personal icon library: ~400 game images, which are game art.
master_aoe2_images/

# Saved settings are per-machine - mine should not become anyone's default.
config.json

# The project venv. Python writes .venv/.gitignore itself, but a venv under
# any other name should stay out too.
.venv/
venv/

__pycache__/
*.py[cod]
.pytest_cache/
.DS_Store
